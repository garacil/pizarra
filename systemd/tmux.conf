# Keep the dedicated process boundary alive even while the registry has no
# local teams. The pizarra watchdog connects to this server and creates only
# explicitly configured sessions.
set-option -g exit-empty off
